describe.skip('GET /worker/jobs', () => {
  test('returns WorkerJobView[]', () => {});
  test('sources results from job_candidates instead of raw jobs', () => {});
  test('does not return the same job twice in the same impression window', () => {});
});
