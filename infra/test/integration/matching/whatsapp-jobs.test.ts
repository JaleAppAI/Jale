describe.skip('WhatsApp JOBS command', () => {
  test('returns matched job IDs from job_candidates', () => {});
  test('limits delivery to 3 jobs per response', () => {});
  test('enforces the 3 jobs per worker per 24h throttle', () => {});
});
