describe.skip('GET /employer/jobs/:id/candidates', () => {
  test('returns EmployerCandidateView[] without phone, coordinates, score internals, or full last names', () => {});
  test('shows matched workers who have not applied yet', () => {});
  test('does not expose workers through jobs owned by other employers', () => {});
});
